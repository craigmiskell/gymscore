* Re-order recording sheet columns to match spreadsheet examples.
  * Add rotation order?
* Export/import a single competition; needs to export enough of competitors and gyms to import correctly without assuming PKs (id).  The import will need to look for existing items and use the local PKs.  Or: this is not a native Dexie export/import, it is a careful data driven algorithm.  This will be for sharing between users.  Needs version handling as well, but do that with an explicit version in the export format (not the version of GymScore)
* Questions for users:
    * Should results entry be per Step (and Division?).  Alternatively: if they are for multi-step, order by step and perhaps display that fact more clearly (deliniation)
    * Should the results modal have national id and club in the left column?  It expands the display quite a bit (3 lines in worst cases), and another column makes it wider.  Does it add benefit, or is name enough?  It matches the recorder sheets (sort of).
    * WDYT about the recorder sheets having #/Name/Club in columns, unlike the original examples (more like the recording *UI*)?
* Can we edit the name of a competitor in the middle of a competition, e.g. we find a typo?
* Unit tests
* General cleanup refactor, with unit tests to validate.
* Collate all production licenses and put them in an 'about' page/window.  Show "NOTICE" files from any Apache 2.0; check other license types for any other requirements
* Check if we have actually ended up  using jquery; remove if not.
* Think about another UI for the front page: set of "unfinished" competitions; click one to go to a Comp page that offers further links to preparing, printing sheets/programmes/etc, collecting results, and printing results + certificates.
